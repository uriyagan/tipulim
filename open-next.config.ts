import { defineCloudflareConfig } from "@opennextjs/cloudflare";

// OpenNext adapter that builds the Next.js app for the Cloudflare Workers
// runtime. See https://opennext.js.org/cloudflare
export default defineCloudflareConfig();
